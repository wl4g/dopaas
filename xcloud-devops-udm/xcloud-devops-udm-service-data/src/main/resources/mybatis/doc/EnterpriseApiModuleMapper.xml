<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- Generated by XCloud DevOps for Codegen, refer: http://dts.devops.wl4g.com -->

<mapper namespace="com.wl4g.devops.doc.data.EnterpriseApiModuleDao">
    <resultMap id="BaseResultMap" type="com.wl4g.devops.common.bean.doc.EnterpriseApiModule">
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="version_id" jdbcType="BIGINT" property="versionId" />
        <result column="parent_id" jdbcType="BIGINT" property="parentId" />
        <result column="name" jdbcType="VARCHAR" property="name" />
    </resultMap>

    <sql id="Base_Column_List">
        id, version_id, parent_id, name
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
               <include refid="Base_Column_List" />
        FROM doc_ee_api_module
        <where>
            id = #{id, jdbcType=BIGINT}
        </where>
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        DELETE FROM doc_ee_api_module
        <where>
            id = #{id, jdbcType=BIGINT}
        </where>
    </delete>

    <insert id="insertSelective" parameterType="com.wl4g.devops.common.bean.doc.EnterpriseApiModule">
        INSERT INTO doc_ee_api_module
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="versionId != null">
                version_id,
            </if>
            <if test="parentId != null">
                parent_id,
            </if>
            <if test="name != null">
                name,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
           <if test="id != null">
               #{id, jdbcType=BIGINT},
           </if>
           <if test="versionId != null">
               #{versionId, jdbcType=BIGINT},
           </if>
           <if test="parentId != null">
               #{parentId, jdbcType=BIGINT},
           </if>
           <if test="name != null">
               #{name, jdbcType=VARCHAR},
           </if>
        </trim>
    </insert>

    <update id="updateByPrimaryKeySelective" parameterType="com.wl4g.devops.common.bean.doc.EnterpriseApiModule">
        UPDATE doc_ee_api_module
        <set>
            <if test="id != null">
                id = #{id, jdbcType=BIGINT},
            </if>
            <if test="versionId != null">
                version_id = #{versionId, jdbcType=BIGINT},
            </if>
            <if test="parentId != null">
                parent_id = #{parentId, jdbcType=BIGINT},
            </if>
            <if test="name != null">
                name = #{name, jdbcType=VARCHAR},
            </if>
        </set>
        <where>
            id = #{id, jdbcType=BIGINT}
        </where>
    </update>

    <update id="updateByPrimaryKey" parameterType="com.wl4g.devops.common.bean.doc.EnterpriseApiModule">
        UPDATE doc_ee_api_module
        <set>
            id = #{id, jdbcType=BIGINT},
            version_id = #{versionId, jdbcType=BIGINT},
            parent_id = #{parentId, jdbcType=BIGINT},
            name = #{name, jdbcType=VARCHAR},
        </set>
        <where>
            id = #{id, jdbcType=BIGINT}
        </where>
    </update>

    <select id="list" resultMap="BaseResultMap" parameterType="com.wl4g.devops.common.bean.doc.EnterpriseApiModule" >
        SELECT
            <include refid="Base_Column_List"/>
        FROM doc_ee_api_module
        <where>
            <if test="enterpriseApiModule.versionId != null" >
                AND `version_id` = #{enterpriseApiModule.versionId}
            </if>
            <if test="enterpriseApiModule.parentId != null" >
                AND `parent_id` = #{enterpriseApiModule.parentId}
            </if>
            <if test="enterpriseApiModule.name != null and enterpriseApiModule.name != ''" >
                AND `name` LIKE CONCAT('%','${enterpriseApiModule.name}','%')
            </if>
        </where>
    </select>

    <select id="getByVersionIdAndParentId" resultMap="BaseResultMap" parameterType="java.util.Map">
        SELECT
        <include refid="Base_Column_List"/>
        FROM doc_ee_api_module
        <where>
            <if test="versionId != null">
                AND `version_id` = #{versionId}
            </if>
            <if test="parentId != null">
                AND `parent_id` = #{parentId}
            </if>
        </where>
    </select>

</mapper>